# ==========================================
# 审稿 Agent 项目专属 .gitignore
# ==========================================

# 1. 极其重要的机密文件！
# 哪怕你现在把 Key 写在了 bashrc 里，以后一旦在项目里建了 .env 文件，
# 这行规则能保证你的大模型 Key 绝对不会被意外保存或泄露
.env
*.key

# 2. Python 运行产生的缓存和编译垃圾
__pycache__/
*.py[cod]
*$py.class

# 3. 虚拟环境（防止把整个 Conda 环境打包进去）
venv/
env/
.conda/

# 4. Trae / VS Code 等 IDE 的本地工作区配置
.vscode/
.trae/
.idea/

# 5. 你的业务输入输出文件
# 假设你以后会把待审阅的论文放在特定文件夹，或者生成大量的 Markdown 报告
*.pdf
*.md
# 如果你建了专门放结果的文件夹，也可以把整个文件夹忽略：
outputs/
# test_papers/